<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="EroMangaManager.WinUI3.UserControls.MangaDisplayNameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EroMangaManager.WinUI3.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{x:Bind Manga}"
             
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <local:CoverWithContextFlyout Grid.Column="0" Source="{x:Bind Manga ,Mode=OneWay}" />
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock
                FontSize="20"
                Text="{x:Bind Manga.FolderPath, Mode=OneWay}"
                TextWrapping="Wrap" />
            <TextBlock
                Grid.Row="1"
                FontSize="20"
                Text="{x:Bind Manga.FileFullName, Mode=OneWay}"
                TextWrapping="Wrap" />
            <TextBlock Grid.Row="2" Text="{x:Bind Manga.FileSize, Mode=OneWay, Converter={StaticResource filesizeConverter}}" />

        </Grid>
    </Grid>
</UserControl>